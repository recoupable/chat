/**
 * Tailwind class that lifts the profile dropdown (and its submenus) above the
 * mobile SideModal (z-[200]) so its items stay reachable on phones. Scoped here
 * so other dropdowns keep the shared primitive's default layer.
 */
const PROFILE_MENU_LAYER = "z-[210]";

export default PROFILE_MENU_LAYER;
